<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Servicios.Views.Recuperar">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Recuperar clave"
                VerticalOptions="Center" 
                HorizontalOptions="Center" Margin="0,30,0,0" 
                   FontAttributes="Bold"
                   />
            <Button Text="imagen!" Margin="100,50,100,0"
                    Padding="0,0,0,100"/>
            <Entry x:Name="usuario" Placeholder="Numero de celular"  Margin="20,10,20,0" Keyboard="Numeric"/>
            <Button Text="Enviar" TextColor="White" HorizontalOptions="Fill" BackgroundColor="#0fabbc" Margin="20,20,20,0"
                   />
            <Button Text="¿Necesitas una cuenta?" TextColor="White" HorizontalOptions="Fill" BackgroundColor="#0fabbc" Margin="20,50,20,0"
                  Clicked="registro"  />
            
        </StackLayout>
    </ContentPage.Content>
</ContentPage>